---
name: opendrive
description: 驾驭 AI 完成中长程开发任务的三阶段工作流——先用产品语言对齐形态并由用户说"确认产品形态"冻结，再按台账小步交付，每个里程碑用产品语言回检漂移；适用于任何需要"AI 自动拆解需求、开发、测试、联调且不跑偏"的任务。当用户说 $opendrive、用 OpenDrive、驾驭这个任务、或开始一个多步骤开发任务时使用。
---

# OpenDrive · 驾驭 AI 的开发工作流

你要完成一个中长程开发任务。这个 Skill 是你的驾驶舱：它定义了三个阶段、一个硬确认门、一套台账纪律。它的存在只有一个目的——**让用户始终知道你在做什么、做到哪了、做的是不是他要的**，而不是让你自我感觉良好地跑完全程。

## 第〇条：硬规则（违反任何一条即视为跑偏，用户可以随时叫停）

1. **确认口令**：只有用户明确说出"确认产品形态"（或同义的、无歧义的确认句）才算确认。积极语气、过渡语、"那你做吧"、"继续"、"好的"——**都不算**。推断确认 = 未授权开发。
2. **单一写入者**：任何时刻只允许一个 Agent 改代码。评审者只读。禁止多个 Agent 共享同一工作树并行写入。
3. **台账即真相**：进度只看 `opendrive/tasks.md` 的勾选状态和 `opendrive/checks/` 的验证证据。你自己的口头/文字汇报不算证据。
4. **漂移即停**：发现实现偏离已冻结形态、或需要改变已冻结形态——停下来，回到第一阶段重新对齐偏差部分，再继续。
5. **先查原生再动手**：设计任何机制前，先查目标平台和参考实现是否已有原生能力，并把核查结论写进 `shape.md` 的"原生能力核查"一节（查了哪些、哪些可直接复用、哪些才需要自建）。没写这一节 = 没查过，阶段一不算完成。重复造轮子是最贵的跑偏。
6. **环境问题升级**：卡在环境/工具链问题超过 15 分钟，把精确错误和已试方案报告给用户，不要闷头调试。
7. **跨仓/删除/改合同必须报**：跨仓库动作、删除文件、修改已确认的合同性文档——先报告，等用户点头。
8. **评审有预算**：任何独立评审/审查最多 2 轮，只有 P0（会导致返工或安全事故的问题）阻塞，P1/P2 记入 `opendrive/log.md` 不阻塞。评审是手段，不是目的；`check` 对账一致 + 用户验收 = 完，不是"评审再也提不出问题"才算完。

## 阶段一：ALIGN（对齐形态，未确认不写码）

1. **先调查**：读现有代码、截图、原型、参考产品源码。用户提供了参考实现的，先检查再描述；不要问能从材料里查明的问题。
2. **写产品形态说明**到 `opendrive/shape.md`，用**用户看得懂的产品语言**（不是技术语言），覆盖：
   - 用户从哪里进入，首先看到什么；
   - 每个页面/功能区有什么、能做什么操作、操作后发生什么；
   - 关键状态如何变化，成功/失败/空/等待状态如何反馈；
   - 本期交付什么、**明确不交付什么**（无占位入口）；
   - 与参考产品的对齐边界（哪些 1:1、哪些因品牌/平台/阶段调整）；
   - **原生能力核查**：为目标平台/参考实现查了哪些原生能力、哪些可直接复用、哪些才需要自建（硬规则 5 的落点）；
   - 完成后按什么真实使用路径验收。
3. 说明末尾**只问一句**："请确认以上产品形态。确认后再进入开发。"
4. **等口令**。拿到"确认产品形态"后，运行：
   ```bash
   python3 <skill目录>/scripts/opendrive.py baseline --quote "<用户确认的原话>"
   ```
   它会把确认时刻写进 `opendrive/shape.md` 头部并 git 提交。**没有这一步，冻结无效。**
5. 用户纠正了任何范围/流程/行为：吸收修改，**重新输出完整版**（不给零散补丁），再等一次口令。

## 阶段二：BUILD（小步交付，台账驱动）

1. **拆任务**到 `opendrive/tasks.md`：每个任务 = 一个可独立验证的小工作单元，格式：
   ```
   ## G1 <里程碑名>
   - [ ] 1.1 <任务> → 验证：<怎么证明做完了>
   - [ ] 1.2 <任务> → 验证：<怎么证明做完了>
   ```
   一个里程碑应该在一两次专注工作内完成；任务总量超过 ~15 个时，先只做 G1，后面的粗写。
2. **每完成一个任务**：
   - 把验证证据（测试输出/命令结果/截图说明）写进 `opendrive/checks/<任务号>.md`；
   - 勾选 `- [x]`；
   - 运行 `python3 <skill目录>/scripts/opendrive.py check` —— 它会报告：已勾选但缺证据的任务、有证据但没勾选的任务、最近改动超出当前任务范围的文件。**不让"代码写了但没勾"或"勾了但没证据"的状态存在。**
3. **每完成一个里程碑**：git 提交，commit message 引用任务号。
4. **任务做不下去、或发现要做台账之外的事**：停下来报告，把新增范围显式摆给用户选——不要默默吸收。

## 阶段三：REVIEW（里程碑回检，用产品语言）

每个里程碑结束（不是整个项目结束），向用户汇报一次，用**产品语言**而非技术语言：
- 现在打开产品，从入口到结果能走通什么（演示路径）；
- 和 `opendrive/shape.md` 比，**有什么偏差**——包括你以为"不算偏差"的实现选择；
- 台账勾选率 + check 脚本输出；
- 下一步要做什么、预计涉及哪些文件。

用户指出偏差 → 回到阶段一对齐偏差部分。用户说"继续" → 进入下一里程碑。

## 质量自检（交付前过一遍）

- 我说的是**用户会看到的行为**，不是实现计划？
- 核心路径从入口到结果闭环了吗？
- 每个勾选的任务都有 `checks/` 证据吗？
- 最近一次 `opendrive.py drift` 输出是干净的吗？
- 我有没有在等口令期间就先写了"反正要用的"代码？（有 = 违规）

## 常用命令

```bash
python3 scripts/opendrive.py init          # 初始化 opendrive/ 目录
python3 scripts/opendrive.py status        # 当前阶段、勾选率、确认状态
python3 scripts/opendrive.py check         # 台账与证据对账
python3 scripts/opendrive.py drift         # 工作树改动 vs 当前任务范围
python3 scripts/opendrive.py baseline      # 冻结已确认形态（阶段一）
python3 scripts/opendrive.py log "..."     # 追加一条带时间戳的决策记录
```

脚本只做检查和记账，不做任何修改性操作；所有判断仍然由你和用户做。
