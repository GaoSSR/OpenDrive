# OpenDrive Roadmap —— 未解决问题与优化上下文

> 这份文件是接续开发入口：记录 OpenDrive 当前版本**没解决的问题**、每条问题对应的**实战证据**、以及下次迭代该往哪走。背景全部来自 2026-09 一次真实失败的 AI 驾驭案例（XWorker 看板协作开发，5 次会话、约 2100 万 Token、OpenSpec 任务勾选 0/43 收场）。

---

## 一、当前版本已解决的问题（对账基线）

来自根因分析的 10 条根因，当前版本硬解了 6 条、半解 2 条、解不了 2 条：

| # | 根因 | 状态 | 机制 |
|---|---|---|---|
| 1 | 对齐错了客体（审技术文档不审产品形态） | ✅ 硬解 | 阶段一 ALIGN + `shape.md` 产品语言模板 |
| 2 | 需求飞奔、流程假设能冻结 | ✅ 硬解 | 硬规则 4 漂移即停 + baseline 可重新冻结 |
| 3 | 递归评审无停止条件 | ✅ 硬解 | 硬规则 8（≤2 轮、仅 P0 阻塞）+ `check` 对账即"完"的定义 |
| 4 | AI 推断授权（把"继续"当确认） | ✅ 硬解 | 确认口令 + `baseline` 脚本级门禁 |
| 6 | 台账与代码脱钩 → 谎报 | ✅ 硬解 | `check`（勾了没证据 ✗ / 有证据没勾 ✗ / 证据为空 ✗） |
| 7 | 格式校验被当"方向正确"汇报 | ✅ 硬解 | `status`/`check` 只说事实，没有"校验通过"话术 |
| 5 | 多写入者并发共享工作树 | ⚠️ 半解 | 只有硬规则 2，无强制机制 → 见问题 P1 |
| 8 | 不查原生能力就造轮子 | ⚠️ 半解 → 已升级 | `baseline` 前强制"原生能力核查"节有实质内容 |
| 9 | 上下文压缩与移交混乱 | ❌ 解不了 | 运行时问题 → 见问题 P2（缓解思路） |
| 10 | 人的管理方式自相矛盾 | ❌ 解不了 | 工具管不了人 → 见问题 P3（使用纪律） |

---

## 二、待解决问题（按优先级）

### P1 · 单一写入者没有强制机制

**现状**：硬规则 2 写了"任何时刻只允许一个 Agent 写代码"，但脚本无法阻止另一个会话改同一文件。实战证据：开发线程/监督线程/主会话三方在同一 git 工作树互相覆盖 `index.ts`（会话行 15446），监督线程 6 次"撤回并行线程越界改写"。

**优化方向**（两选一或都做）：
- `opendrive.py lock --session <id>`：在 `opendrive/` 下放锁文件（含会话 ID + 时间戳 + 主机名），`status`/`check` 检测到别人的锁就报警并退出非零。防君子不防小人，但能把"有人在写"变成可见状态。
- 文档层面给出标准做法：多 Agent 并行时用 `git worktree` 给每个 Agent 独立工作树，OpenDrive 的 `drift` 天然按工作树隔离。

**验收**：起两个会话同时跑，`status` 能明确显示"另一个会话 session-X 正在写入"。

### P2 · 上下文压缩导致约束丢失（缓解）

**现状**：运行时问题，工具管不到。实战证据：限流移交做错了对象（开发线程被移交、监督线程留下）；旧约束随移交丢失（"Git 只读"旧规则被新会话当成有效）；同一份参考源码因压缩被反复全量重读。

**优化方向**：
- 把"当前阶段、已冻结形态摘要、当前里程碑 scope、未完成任务"压缩成一段 < 500 字的 `opendrive/handoff.md`，`status --handoff` 输出。新会话第一句话就是读它，而不是重读全部历史。
- SKILL.md 增加一条：任务拆到"单个上下文窗口能交付一个里程碑"的粒度，超了就先切。

**验收**：模拟压缩后开新会话，只读 `handoff.md` 就能正确继续，不需要翻历史。

### P3 · 委托与微观管理的边界（使用纪律文档）

**现状**：工具管不了人，但可以把"委托时看什么"变简单。实战证据：用户凌晨惊醒检查、威胁评分淘汰；AI 因此采取最保守行为（不停汇报、不停审查），每一步烧 Token。

**优化方向**：
- 在 README 增一节"委托模式使用指南"：委托前定义"什么情况必须叫醒我"（跨仓动作、删除、改 shape.md、check 连续失败 ≥2 次、Token/时间超预算），写进 `opendrive/log.md`；委托期间只看 `status`/`check` 输出，不听汇报。
- 可选：`opendrive.py watch`——一个常驻命令，只在触发上述条件时输出一行，否则静默。让用户可以放心离开。

### P4 · 证据时效性（checks 过期检测）

**现状**：`check` 只看证据存在且非空，不看证据是否比代码旧。实战证据：AI 拿半小时前的测试通过记录为刚改的代码背书。

**优化方向**：`check` 对比 `checks/<id>.md` 的 mtime 和该任务 scope 内最新代码改动的 mtime，证据旧于代码就标 `△ 证据已过期`。

### P5 · 双 Agent 行为一致性验证

**现状**：SKILL.md 同时装给 Codex 和 Claude Code，但两个 Agent 对"确认口令""台账即真相"的执行严格程度未经过对照测试。

**优化方向**：准备一个标准化测试任务（如"给一个 Flask demo 加一个 /health 端点"），分别在两个 Agent 里跑完整三阶段，对比它们在哪一步开始违规，把差异补进 SKILL.md 措辞。

### P6 · 与 OpenSpec 的关系澄清

**现状**：OpenDrive 和 OpenSpec 可以并存（OpenSpec 管技术合同，OpenDrive 管驾驭节奏），但 SKILL.md 没写共存时谁先谁后、信息放哪边，用户可能混淆。

**优化方向**：README 增一节"与 OpenSpec 共存"：shape.md 引用 OpenSpec proposal 而不是复制；tasks.md 的任务号可映射到 OpenSpec tasks；避免双份台账。

---

## 三、关键设计决策记录（为什么长这样）

1. **为什么是 Skill + 脚本而不是 CLI 工具**：用户的核心痛点是"AI 行为跑偏"，行为约束必须进 AI 的上下文（SKILL.md），脚本只做 AI 做不到的机械对账。完整 CLI 会把流程变成又一个需要学习的软件。
2. **为什么确认口令是脚本级门禁而不是 SKILL.md 一句话**：9-15 会话证明写在 AGENTS.md 里的规则换项目就丢；`baseline` 拒绝执行是物理防线。
3. **为什么证据必须落盘成文件而不是 AI 口头汇报**：9-18 会话的"Electron 已开发完成"谎报（实际 251 行只读组件）证明口头完成声明零成本。
4. **为什么 drift 用 scope 注释而不是解析代码依赖**：简单、可读、可手改；AI 改 scope 也是一次显式的范围变更，会留在 git 历史里。

---

## 四、上手继续开发

```bash
git clone https://github.com/GaoSSR/OpenDrive.git
cd OpenDrive
bash scripts/install.sh        # 装到本机 Codex/Claude Code
# 改动后自测：
cd /tmp && mkdir od-test && cd od-test && git init
python3 <repo>/scripts/opendrive.py init
```

开发纪律：改 SKILL.md 的硬规则时，同步改 README 的硬规则摘要；改 opendrive.py 的行为时，在 /tmp 里把相关分支都跑一遍。
